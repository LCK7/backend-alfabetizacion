# Ejemplo de variables de entorno (NO subir secretos a VCS)
# Para Railway pega estas variables en Settings > Variables

# Cadena de conexión de Railway (internal) o pública
DATABASE_URL=postgresql://user:pass@host:port/dbname
# Alternativa pública si la tienes
DATABASE_PUBLIC_URL=postgresql://user:pass@host:port/dbname

# Si Railway requiere SSL en la conexión (setear true en producción)
DB_SSL=true

# JWT secreto para firmar tokens
JWT_SECRET=71850190Luis

# Puerto opcional
PORT=5000
